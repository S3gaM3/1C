{
  "mcpServers": {
    "answer42": {
      "type": "stdio",
      "command": "answer42",
      "env": {
        "ONEC_MCP_CREDENTIALS_FILE": "${userHome}/.answer42-credentials.json"
      }
    }
  }
}
