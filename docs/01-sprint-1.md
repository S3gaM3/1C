# 1. Спринт 1 — объекты учёта

Цель спринта: хранение справочников, три документа, два регистра накопления, движения **конструктором**, печатная форма реализации, функциональная опция складов. Код модулей — **на русском**.

После каждого крупного блока: `Ctrl+S`, затем `F7` (принять реорганизацию). Предприятие для проверки карточек запускайте через **Отладка** → **Начать отладку**, когда объект уже создан.

Содержание:

- 1.1 Перечисления
- 1.2 Должности
- 1.3 Сотрудники (Таб.№ и Ф.И.О.)
- 1.4 Организации
- 1.5 Склады
- 1.6 Породы питомцев
- 1.7 Источники привлечения
- 1.8 Контрагенты
- 1.9 Договоры
- 1.10 Номенклатура
- 1.11 Пользователи
- 1.12 Документ «Запись клиента»
- 1.13 Документ «Реализация товаров и услуг»
- 1.14 Документ «Поступление товаров»
- 1.15 Регистр «Товары на складах»
- 1.16 Регистр «Продажи»
- 1.17 Движения конструктором
- 1.18 Печатная форма
- 1.19 Функциональная опция складов
- 1.20 Проверка в предприятии

---

## 1.1. Перечисления — что это и куда их класть

Это **не** справочники, **не** документы и **не** строки, которые потом вбивают в карточку клиента.

**Перечисление** — короткий фиксированный список вариантов для выпадающего списка (позже на форме спринта 2 — ещё и переключатель из двух кнопок). Сами значения вы создаёте один раз в Конфигураторе. В режиме 1С:Предприятие пользователь только **выбирает** «Клиент» или «Поставщик», новые пункты туда не добавляет.

**Куда:** левое дерево конфигурации → ветка **Перечисления** (рядом со Справочниками, ниже корня «ГрумингСалон»).

Нужно **пять отдельных** перечислений. Каждое — свой объект. `Клиент` и `Поставщик` — это не два перечисления, а **два значения внутри одного** перечисления `ТипыКонтрагентов`.

### Как создать одно перечисление (повторите 5 раз)

1. Слева найдите строку **Перечисления**. Если дерева нет: меню **Конфигурация** → **Открыть конфигурацию**.
2. Правая кнопка мыши по **Перечисления** → **Добавить**.
3. Справа откроется палитра свойств. Заполните **Имя** (без пробелов) и **Синоним** (с пробелами, как в таблице ниже).
4. В окне перечисления откройте вкладку **Данные** (не «Формы», не «Макеты»).
5. Над списком значений нажмите **Добавить** (зелёный «+»). Для каждой строки заполните **Имя** и **Синоним**.
6. `Ctrl+S`, затем `F7`.

Пока **не** открывайте 1С:Предприятие и **не** ищите эти слова в карточках. К формам они привяжутся позже, когда у справочника появится реквизит с типом `ПеречислениеСсылка.…`.

| Перечисление (имя объекта) | Синоним объекта | Значения: имя → синоним | Зачем потом |
| --- | --- | --- | --- |
| `ТипыОрганизаций` | Типы организаций | `ЮридическоеЛицо` → Юридическое лицо; `ИндивидуальныйПредприниматель` → Индивидуальный предприниматель | реквизит Организации «Тип организации» |
| `ТипыКонтрагентов` | Типы контрагентов | `Клиент` → Клиент; `Поставщик` → Поставщик | реквизит Контрагенты «Тип контрагента» |
| `ВидыПитомцев` | Виды питомцев | `Кошка` → Кошка; `Собака` → Собака | порода и ТЧ «Питомцы» |
| `Полы` | Полы | `Мужской` → Мужской; `Женский` → Женский | сотрудник и клиент |
| `ТипыНоменклатуры` | Типы номенклатуры | `Услуга` → Услуга; `Товар` → Товар; `Материал` → Материал | номенклатура: услуга / товар / материал |

### ТипыОрганизаций

1. ПКМ **Перечисления** → **Добавить**.
2. Имя: `ТипыОрганизаций`. Синоним: `Типы организаций`.
3. Вкладка **Данные** → два значения:
   - Имя `ЮридическоеЛицо`, синоним `Юридическое лицо`
   - Имя `ИндивидуальныйПредприниматель`, синоним `Индивидуальный предприниматель`

### ТипыКонтрагентов

1. Снова ПКМ **Перечисления** → **Добавить** (новый объект, не открывайте предыдущий).
2. Имя: `ТипыКонтрагентов`. Синоним: `Типы контрагентов`.
3. Вкладка **Данные** → два значения:
   - Имя `Клиент`, синоним `Клиент`
   - Имя `Поставщик`, синоним `Поставщик`

В дереве слева должно получиться: `Перечисления` → `ТипыКонтрагентов` → внутри значения `Клиент` и `Поставщик`.

### ВидыПитомцев

1. ПКМ **Перечисления** → **Добавить**.
2. Имя: `ВидыПитомцев`. Синоним: `Виды питомцев`.
3. Вкладка **Данные**:
   - Имя `Кошка`, синоним `Кошка`
   - Имя `Собака`, синоним `Собака`

Не путайте с справочником **Породы питомцев** (его создадите в п. 1.6): там будут «Британская», «Болонка». Здесь только вид: кошка или собака.

### Полы

1. ПКМ **Перечисления** → **Добавить**.
2. Имя: `Полы`. Синоним: `Полы`.
3. Вкладка **Данные**:
   - Имя `Мужской`, синоним `Мужской`
   - Имя `Женский`, синоним `Женский`

Это общий список для сотрудников и клиентов. Отдельное перечисление «Пол клиента» создавать не нужно.

### ТипыНоменклатуры

1. ПКМ **Перечисления** → **Добавить**.
2. Имя: `ТипыНоменклатуры`. Синоним: `Типы номенклатуры`.
3. Вкладка **Данные** → **три** значения:
   - Имя `Услуга`, синоним `Услуга`
   - Имя `Товар`, синоним `Товар`
   - Имя `Материал`, синоним `Материал`

После этого в дереве под **Перечисления** ровно пять объектов. Если видите «Клиент» на одном уровне с «ТипыКонтрагентов» — вы создали лишнее перечисление вместо значения. Удалите его (ПКМ → Удалить) и добавьте `Клиент` как значение на вкладке **Данные** у `ТипыКонтрагентов`.

---

## 1.2. Справочник Должности

ТЗ: только стандартные **Код** и **Наименование**. Свои реквизиты не добавляйте.

1. В дереве ПКМ по ветке **Справочники** → **Добавить**.
2. Откроется окно «Справочник». В палитре свойств (обычно справа или внизу):
   - **Имя:** `Должности`
   - **Синоним:** `Должности` (подставится сам, если не затрёте)
3. Вкладка **Нумерация** в этом же окне:
   - тип кода: **Строка**
   - длина кода: `9`
   - автонумерация: включена
4. Вкладка **Данные**:
   - **Длина наименования:** `150`
5. Закройте окно справочника крестиком, согласитесь сохранить. `Ctrl+S`, `F7`.

Пока ничего не пишите в 1С:Предприятие. Сначала создайте остальные справочники.

---

## 1.3. Справочник Сотрудники

### Что здесь вообще меняют

У каждого справочника платформа уже даёт два поля, их **не нужно создавать плюсиком**:

| Служебное имя (не меняется) | Зачем | Что должно быть написано на форме в макете ТЗ |
| --- | --- | --- |
| `Код` | табельный номер | **Таб.№** |
| `Наименование` | полное ФИО одной строкой | **Ф.И.О.** |

Это и есть **стандартные реквизиты**. Вы меняете только подпись (свойство **Синоним**), а не имя объекта и не имя справочника.

Не делайте так:

- не создавайте новый реквизит с именем `ТабНомер` или `ФИО`;
- не пишите `Таб.№` в имя самого справочника (справочник по-прежнему `Сотрудники`);
- не ищите эти слова в режиме 1С:Предприятие, пока не сделали `F7` — их нет в карточках «просто так», это подписи полей.

### Шаг A. Создать справочник

1. ПКМ **Справочники** → **Добавить**.
2. **Имя:** `Сотрудники`. **Синоним:** `Сотрудники`.
3. Вкладка **Нумерация:** тип кода Строка, длина `9`, автонумерация.
4. Вкладка **Данные:** **Длина наименования** = `150`.  
   Это поле именно на вкладке **Данные** окна справочника, не в «стандартных реквизитах».

### Шаг B. Открыть список стандартных реквизитов (сюда вводить Таб.№ и Ф.И.О.)

**Не трогайте** верхнее поле **Синоним: Сотрудники** — это имя всего справочника, не табельный номер.

Смотрите **правую** панель «Свойства: Сотрудники». Прокрутите её вниз, в группе **Данные**, сразу под строкой **Автонумерация**.

Там есть строка:

**Стандартные реквизиты** ………… кнопка **Открыть**

1. Нажмите именно **Открыть** (в других сборках вместо слова «Открыть» бывает лупа).
2. Появится отдельное окно со списком: `Код`, `Наименование`, `ПометкаУдаления` и т.д.
3. Дважды щёлкните строку **Код**.
4. Палитра справа сменит заголовок на свойства **Кода**. Найдите **Синоним** (сейчас там `Код`).
5. Замените на `Таб.№` и щёлкните любое другое поле, чтобы значение зафиксировалось.
6. Вернитесь в список, дважды щёлкните **Наименование**, синоним = `Ф.И.О.`

Пока не нажали **Открыть**, поля «Синоним Кода» в окне справочника **нет** — его специально спрятали внутрь этого списка.

На том же правом списке свойств справочника сразу поправьте:

- **Иерархический** — снимите флажок. У сотрудников не должно быть групп и «родителя».
- **Длина наименования** — поставьте `150`, не `25` (иначе длинное ФИО не влезет).

### Шаг C. Синоним Кода = Таб.№

После **Открыть** у стандартных реквизитов:

1. Дважды щёлкните **Код**.
2. В палитре свойств реквизита Код найдите **Синоним**.
3. Впишите `Таб.№` (буква Т, точка, знак №).

Имя реквизита так и останется `Код`. Меняется только подпись на карточке.

### Шаг D. Синоним Наименования = Ф.И.О.

1. В том же списке после **Открыть** дважды щёлкните **Наименование**.
2. **Синоним** = `Ф.И.О.`
3. Закройте окна, `Ctrl+S`, `F7`.

На макете карточки сотрудника сверху будет:

- слева подпись **Таб.№:** — это стандартный Код;
- слева подпись **Ф.И.О.:** — это стандартное Наименование (пока обычное поле; на спринте 2 его заблокируют и будут собирать из Фамилия + Имя + Отчество).

### Шаг E. Свои реквизиты (уже плюсиком, вкладка Данные)

Это **другие** поля. Их нет среди стандартных. Откройте справочник `Сотрудники` → вкладка **Данные** → над таблицей реквизитов зелёный **+**.

Для каждого: имя, синоним, тип. Обязательность: свойство **Проверка заполнения** = **Выдавать ошибку**.

| Имя (без пробелов) | Синоним (подпись на форме) | Тип | Как выбрать тип | Обязательный |
| --- | --- | --- | --- | --- |
| Фамилия | Фамилия | Строка, длина 50 | Тип → Строка → длина 50 | Да |
| Имя | Имя | Строка, 50 | так же | Да |
| Отчество | Отчество | Строка, 50 | так же | Нет |
| Пол | Пол | перечисление Полы | Тип → ссылка на перечисление → `Полы` | Да |
| ДатаРождения | Дата рождения | Дата | Дата, состав «Дата» | Нет |
| Должность | Должность | справочник Должности | Тип → ссылка на справочник → `Должности` | Нет |
| КонтактныйТелефон | Контактный телефон | Строка, 20 | Строка 20 | Нет |

Тип «ссылка на перечисление»: в окне типа слева **ПеречислениеСсылка**, справа выберите `Полы`.  
Тип «ссылка на справочник»: слева **СправочникСсылка**, справа `Должности`.

На спринте 1 форму элемента можно не рисовать: платформа покажет поля сама. На спринте 2 Ф.И.О. станет только для просмотра.

`Ctrl+S`, `F7`.

---

## 1.4. Справочник Организации

1. ПКМ **Справочники** → **Добавить**.
2. **Имя:** `Организации`. **Синоним:** `Организации`.
3. Вкладка **Нумерация:** тип кода Строка, длина `9`, автонумерация.
4. Вкладка **Данные:** длина наименования `150`.
5. На той же вкладке **Данные**, в таблице реквизитов (ещё пустая) нажмите зелёный **+**. Добавьте поля по таблице ниже.
6. После каждого реквизита: выделите его в списке → палитра свойств → **Проверка заполнения** = **Выдавать ошибку**, если в колонке «Обязательный» стоит Да.

Как выбрать тип (окно «Тип данных», кнопка с буквой **T** у свойства Тип):

- **Строка:** слева «Строка», длина 12, переменная.
- **Перечисление:** слева `ПеречислениеСсылка`, справа `ТипыОрганизаций`.
- **Справочник:** слева `СправочникСсылка`, справа `Сотрудники` или `Должности`.

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| ТипОрганизации | Тип организации | ПеречислениеСсылка.ТипыОрганизаций | Да |
| ИНН | ИНН | Строка, 12 | Да |
| Руководитель | Руководитель | СправочникСсылка.Сотрудники | Нет |
| ДолжностьРуководителя | Должность руководителя | СправочникСсылка.Должности | Нет |
| ГлавныйБухгалтер | Главный бухгалтер | СправочникСсылка.Сотрудники | Нет |

Почему Руководитель — ссылка на сотрудника, а не строка: одно ФИО хранится в справочнике «Сотрудники», организация на него ссылается. Так учат на уроках.

Форму пока не создавайте. `Ctrl+S`, `F7`.

---

## 1.5. Справочник Склады

Как Должности: только Код и Наименование, своих реквизитов нет.

1. ПКМ **Справочники** → **Добавить**.
2. **Имя:** `Склады`. **Синоним:** `Склады`.
3. **Нумерация:** строка, длина 9, автонумерация.
4. **Данные:** длина наименования `50` (или 150 — не принципиально).
5. У стандартного реквизита **Наименование** проверка заполнения = Выдавать ошибку (стандартные реквизиты → Наименование → Проверка заполнения).
6. Свои реквизиты **не** добавляйте.

Этот справочник позже включите в функциональную опцию «Использовать склады». `Ctrl+S`, `F7`.

---

## 1.6. Справочник ПородыПитомцев

Здесь три отдельные настройки: иерархия, подпись «Порода», предопределённые **группы** Кошки и Собаки.

### Шаг A. Создать справочник и включить дерево

1. ПКМ **Справочники** → **Добавить**.
2. **Имя:** `ПородыПитомцев`. **Синоним:** `Породы питомцев`.
3. Откройте вкладку **Иерархия** (в окне справочника, рядом с «Данные»).
4. Поставьте флажок **Иерархический справочник**.
5. **Вид иерархии:** `Иерархия групп и элементов` (не «иерархия элементов»).
6. **Размещать группы сверху:** да (если есть такой флажок).
7. Вкладка **Нумерация:** строка, 9, автонумерация.
8. Вкладка **Данные:** длина наименования `100`.

### Шаг B. Подпись Наименования = Порода

Как Таб.№ у сотрудников, только другое слово:

1. Выделите справочник в дереве.
2. Палитра свойств → **Стандартные реквизиты** → лупа.
3. Дважды **Наименование** → свойство **Синоним** = `Порода`.
4. Проверка заполнения = Выдавать ошибку.

Код оставьте с синонимом «Код».

### Шаг C. Свой реквизит Вид питомца

Вкладка **Данные** → зелёный **+**:

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| ВидПитомца | Вид питомца | ПеречислениеСсылка.ВидыПитомцев | Да |

Тип: слева `ПеречислениеСсылка`, справа `ВидыПитомцев`.

### Шаг D. Предопределённые группы Кошки и Собаки

Это не обычные элементы, которые потом вбивают в предприятии. Их заводят **в Конфигураторе**, чтобы они были в каждой базе после `F7`.

1. Дважды откройте справочник `ПородыПитомцев`.
2. Вкладка **Прочее**.
3. Кнопка **Предопределенные** (в центре вкладки).
4. Откроется окно «Предопределенные элементы».
5. На панели этого окна нажмите **Добавить группу** (иконка папки). **Не** «Добавить» (элемент без папки).
6. В палитре свойств группы:
   - **Имя:** `Кошки` (без пробелов, для кода)
   - **Наименование:** `Кошки` (то, что видно пользователю)
7. Снова **Добавить группу**:
   - **Имя:** `Собаки`
   - **Наименование:** `Собаки`
8. Закройте окно предопределённых, сохраните справочник.

После `F7` в 1С:Предприятие откройте Породы питомцев → **Ещё** → **Режим просмотра** → **Дерево**. Должны быть две папки. Породы (Абиссинская и т.д.) заведёте уже в предприятии, внутрь этих папок — см. файл с данными.

`Ctrl+S`, `F7`.

---

## 1.7. Справочник ИсточникиПривлеченияКлиентов

Откуда клиент узнал о салоне. Только Код и Наименование.

1. ПКМ **Справочники** → **Добавить**.
2. **Имя:** `ИсточникиПривлеченияКлиентов`.
3. **Синоним:** `Источники привлечения клиентов`.
4. **Нумерация:** строка, 9, авто.
5. **Данные:** длина наименования `150`. Свои реквизиты не добавляйте.

`Ctrl+S`, `F7`.

---

## 1.8. Справочник Контрагенты

Клиенты и поставщики живут в одном справочнике. Группы **Клиенты** и **Поставщики** — предопределённые папки в Конфигураторе.

Реквизит **ОсновнойДоговор** пока не создавайте: справочника договоров ещё нет. Его добавите в конце п. 1.9.

### Шаг A. Справочник и иерархия

1. ПКМ **Справочники** → **Добавить**.
2. **Имя:** `Контрагенты`. **Синоним:** `Контрагенты`.
3. Вкладка **Иерархия:** поставьте **Иерархический справочник**, вид иерархии **Иерархия групп и элементов**.
4. Вкладка **Нумерация:** тип кода Строка, длина 9, автонумерация.
5. Вкладка **Данные:** длина наименования `150`.

### Шаг B. Предопределённые группы

1. Вкладка **Прочее** → кнопка **Предопределенные**.
2. **Добавить группу** (иконка папки), не обычный элемент.
3. Первая группа: **Имя** `Клиенты`, **Наименование** `Клиенты`.
4. Вторая группа: **Имя** `Поставщики`, **Наименование** `Поставщики`.
5. Закройте окно предопределённых.

### Шаг C. Реквизиты шапки

Вкладка **Данные**, верхняя таблица, зелёный **+**. Для обязательных: палитра свойств реквизита → **Проверка заполнения** = **Выдавать ошибку**.

Неограниченный комментарий: тип Строка, длина `0`.

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| ТипКонтрагента | Тип контрагента | ПеречислениеСсылка.ТипыКонтрагентов | Да |
| КонтактныйТелефон | Контактный телефон | Строка, 20 | Нет |
| ДатаРождения | Дата рождения клиента | Дата, состав «Дата» | Нет |
| Пол | Пол | ПеречислениеСсылка.Полы | Нет |
| Источник | Источник | СправочникСсылка.ИсточникиПривлеченияКлиентов | Нет |
| Комментарий | Комментарий | Строка неограниченная | Нет |

### Шаг D. Табличная часть Питомцы

Таблица внизу карточки клиента — это не отдельный справочник.

1. На вкладке **Данные** найдите нижний блок **Табличные части**.
2. Нажмите **+** именно у табличных частей.
3. **Имя:** `Питомцы`. **Синоним:** `Питомцы`.
4. Щёлкните строку `Питомцы`, чтобы она стала текущей.
5. В списке реквизитов табличной части (обычно справа) жмите **+** и добавьте колонки:

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| ВидПитомца | Вид питомца | ПеречислениеСсылка.ВидыПитомцев | Да |
| ПородаПитомца | Порода питомца | СправочникСсылка.ПородыПитомцев | Да |
| КличкаПитомца | Кличка питомца | Строка, 50 | Нет |
| ДатаРожденияПитомца | Дата рождения питомца | Дата | Нет |

Чтобы в выборе породы были только кошки или только собаки — настройка ниже в блоке «Как нажать Связи параметров выбора». Сначала доделайте договоры (п. 1.9), потом сразу оба отбора.

`Ctrl+S`, `F7`.

---

## 1.9. Справочник ДоговорыКонтрагентов

Договор принадлежит контрагенту-поставщику. Владелец задаётся не плюсиком на вкладке «Данные», а на вкладке **Владельцы**.

### Шаг A. Создать и подчинить

1. ПКМ **Справочники** → **Добавить**.
2. **Имя:** `ДоговорыКонтрагентов`. **Синоним:** `Договоры`.
3. Вкладка **Владельцы** → **+** → выберите справочник `Контрагенты`.
4. **Использование подчинения:** `Элементам` (договор у ООО «Бонифаций», не у папки «Поставщики»).
5. Вкладка **Нумерация:** строка, 9, авто.
6. Вкладка **Данные:** длина наименования `150`.

На спринте 1 наименование можно вводить вручную: `№ 144 от 17.04.2023 г.` На спринте 2 оно станет авто.

### Шаг B. Реквизиты шапки

Вкладка **Данные** → **+**:

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| НомерДоговора | Номер договора | Строка, 20 | Да |
| ДатаЗаключения | Дата заключения договора | Дата, состав «Дата» | Да |
| СрокДействия | Действует до | Дата, состав «Дата» | Нет |
| Бессрочный | Бессрочный | Булево | Нет |
| Комментарий | Комментарий | Строка неограниченная | Нет |

Булево: в окне типа выберите **Булево** — на форме это флажок.

### Шаг C. Реквизит ОсновнойДоговор у контрагента

1. Дважды откройте справочник **Контрагенты**.
2. Вкладка **Данные**, **верхняя** таблица реквизитов (не табличные части внизу) → **+**.
3. **Имя:** `ОсновнойДоговор`. **Синоним:** `Основной договор`.
4. **Тип:** кнопка с буквой T → слева `СправочникСсылка` → справа `ДоговорыКонтрагентов` → ОК.
5. Дальше настройте отбор по инструкции ниже (задача 1).

`Ctrl+S`, `F7`.

---

## Как нажать «Связи параметров выбора» (ОсновнойДоговор и ПородаПитомца)

Это **не** строка **Параметры выбора**. Рядом в палитре есть две похожие строки:

| Строка в свойствах | Когда нужна |
| --- | --- |
| **Параметры выбора** | постоянный отбор, например «всегда только Клиент» |
| **Связи параметров выбора** | отбор **от другого поля этой же карточки** — вот оно |

Для договоров и пород нужна именно **Связи параметров выбора**.

Окно такое же по духу, как **Стандартные реквизиты → Открыть**: строка в правой палитре и кнопка **Открыть**.

### Задача 1. Основной договор — только договоры этого контрагента

Смысл: у Летяевой в списке не должно быть договора ООО «Бонифаций». Владелец договора = текущий контрагент (`Ссылка`).

1. Справочник **Контрагенты** открыт, вкладка **Данные**.
2. В **верхнем** списке реквизитов один раз щёлкните `ОсновнойДоговор` (строка должна подсветиться).
3. Справа палитра сменит заголовок на свойства **этого реквизита** (не всего справочника). Если всё ещё «Свойства: Контрагенты» — щёлкните реквизит ещё раз.
4. Прокрутите правую палитру. Найдите **Связи параметров выбора** (не «Параметры выбора»).
5. Справа нажмите **Открыть**.
6. Пустое окно. Нажмите **Добавить** (зелёный +).
7. Появится строка с двумя колонками. Заполните так:

| Колонка | Что вписать / выбрать |
| --- | --- |
| Имя | `Отбор.Владелец` — наберите с клавиатуры, точка между словами, без пробелов |
| Реквизит (иногда «Путь к данным» или «Значение») | нажмите выбор в ячейке и укажите `Ссылка` |

`Ссылка` — это «этот контрагент», стандартное поле, плюсиком его создавать не нужно.

8. ОК / закрыть окно связей. `Ctrl+S`.

Если в списке нет `Ссылка`: раскройте ветку объекта и ищите стандартный реквизит **Ссылка**.

### Задача 2. Порода питомца — только породы выбранного вида

Смысл: в строке «Кошка» нельзя выбрать «Болонка».

1. Тот же справочник **Контрагенты**, вкладка **Данные**.
2. Внизу щёлкните табличную часть `Питомцы`, чтобы она стала текущей.
3. В списке реквизитов **табличной части** (колонки ТЧ) один раз щёлкните `ПородаПитомца`.
4. Справа — свойства реквизита ТЧ. Строка **Связи параметров выбора** → **Открыть**.
5. **Добавить**. Справа появится строка в таблице **Параметры**.
6. Колонка **Имя** (правая таблица): `Отбор.ВидПитомца`.
7. Слева в **Доступные реквизиты** найдите и щёлкните `Питомцы.ВидПитомца` (не путайте с полями шапки).
8. Нажмите кнопку **>** между списками. В колонке **Реквизит** справа должно появиться `Питомцы.ВидПитомца`.
9. **Режим изменения** оставьте `Очищать` — если сменили вид с кошки на собаку, порода сбросится.
10. **ОК**, затем в Конфигураторе `Ctrl+S` и `F7`.

У справочника **ПородыПитомцев** реквизит должен называться именно `ВидПитомца`. Иначе отбор не к чему привязать.

### Частые ошибки

- Открыли **Параметры выбора** вместо **Связи параметров выбора** — связи там нет, закройте и возьмите соседнюю строку.
- Палитра показывает свойства всего справочника — вы не выделили реквизит в таблице.
- Имя с пробелом: `Отбор. Владелец` не сработает. Только `Отбор.Владелец`.
- Для поступления товаров позже так же: реквизит документа `Договор`, связь `Отбор.Владелец` = реквизит шапки `Поставщик`.

---

## 1.10. Справочник Номенклатура

Услуги, товары и материалы — один справочник. Различает их перечисление **Тип номенклатуры**. Папки Услуги / Товары / Материалы заведите предопределёнными группами.

1. ПКМ **Справочники** → **Добавить**.
2. **Имя:** `Номенклатура`. **Синоним:** `Номенклатура`.
3. Вкладка **Иерархия:** иерархический, иерархия групп и элементов.
4. Вкладка **Нумерация:** строка, 9, авто.
5. Вкладка **Данные:** длина наименования **100** (требование ТЗ, сделайте сразу).
6. **+** реквизит:

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| ТипНоменклатуры | Тип номенклатуры | ПеречислениеСсылка.ТипыНоменклатуры | Да |

7. Вкладка **Прочее** → **Предопределенные** → три раза **Добавить группу**:

| Имя | Наименование |
| --- | --- |
| Услуги | Услуги |
| Товары | Товары |
| Материалы | Материалы |

`Ctrl+S`, `F7`.

---

## 1.11. Справочник Пользователи

На спринте 1 автора документа выбирают вручную из этого справочника. Автоподстановка из сеанса — спринт 2.

1. ПКМ **Справочники** → **Добавить**.
2. **Имя:** `Пользователи`. **Синоним:** `Пользователи`.
3. Вкладка **Данные:** длина наименования **100**.
4. Стандартные реквизиты (лупа у справочника в палитре):
   - **Наименование** → синоним `Имя пользователя`
   - **Наименование** → проверка заполнения = Выдавать ошибку
5. Вкладка **Данные** → **+**:

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| ИдентификаторПользователяИБ | Идентификатор | Строка, 50 | Да |

Не кладите UUID в стандартный **Код**: код обычно короткий (`000000001`), а в макете идентификатор вида `60fcc37c-d795-4353-af76-c42b767bb263`.

`Ctrl+S`, `F7`.

---

## 1.12. Документ ЗаписьКлиента

Документ — это не справочник. В дереве это другая ветка: **Документы**.

### Шаг A. Создать документ

1. ПКМ по ветке **Документы** → **Добавить**.
2. **Имя:** `ЗаписьКлиента`. **Синоним:** `Запись клиента`.
3. Вкладка **Нумерация:** тип номера Строка, длина `9`, автонумерация.
4. Стандартные реквизиты документа **Номер** и **Дата** уже есть, плюсиком их не добавляйте. Дата обязательна: стандартные реквизиты → **Дата** → проверка заполнения = Выдавать ошибку.

### Шаг B. Реквизиты шапки

Вкладка **Данные**, верхняя таблица, **+**.

Тип «дата и время»: в окне типа **Дата**, состав **Дата и время**.

Тип «неограниченная строка»: Строка, длина `0`.

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| Клиент | Клиент | СправочникСсылка.Контрагенты | Да |
| Сотрудник | Сотрудник | СправочникСсылка.Сотрудники | Да |
| ДатаЗаписи | Дата записи | Дата и время | Да |
| Комментарий | Комментарий | Строка неограниченная | Нет |
| Ответственный | Ответственный | СправочникСсылка.Пользователи | Нет |

### Шаг C. Чтобы в Клиенте не выбирались поставщики

1. Выделите реквизит `Клиент`.
2. Палитра свойств → **Параметры выбора** → лупа → **Добавить**.
3. Имя параметра: `Отбор.ТипКонтрагента`.
4. Значение: нажмите выбор → перечисление `ТипыКонтрагентов` → значение `Клиент`.

Это не «связи параметров», а именно **параметры выбора** (фиксированный отбор).

### Шаг D. Табличная часть Услуги

1. Нижний блок вкладки **Данные** — **Табличные части** → **+**.
2. **Имя:** `Услуги`. **Синоним:** `Услуги`.
3. Выделите ТЧ, справа **+** колонки:

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| Услуга | Услуга | СправочникСсылка.Номенклатура | Да |
| Стоимость | Стоимость | Число, длина 15, точность 2 | Да |

Число: в окне типа **Число**, длина 15, точность 2, неотрицательное — по желанию.

Отбор только услуг:

1. Реквизит ТЧ `Услуга` → **Параметры выбора** → лупа → **Добавить**.
2. Имя: `Отбор.ТипНоменклатуры`.
3. Значение: перечисление `ТипыНоменклатуры.Услуга`.

`Ctrl+S`, `F7`. Проведение этому документу на спринте 1 ещё не нужно (регистр заказов появится в спринте 2).

---

## 1.13. Документ РеализацияТоваровИУслуг

### Шаг A. Создать документ

1. ПКМ **Документы** → **Добавить**.
2. **Имя:** `РеализацияТоваровИУслуг`.
3. **Синоним:** `Реализация товаров и услуг`.
4. **Нумерация:** строка, 9, авто. Дата обязательна.

### Шаг B. Реквизиты шапки

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| Основание | Основание | ДокументСсылка.ЗаписьКлиента | Нет |
| Клиент | Клиент | СправочникСсылка.Контрагенты | Да |
| Сотрудник | Сотрудник | СправочникСсылка.Сотрудники | Да |
| Склад | Склад | СправочникСсылка.Склады | Да |
| СуммаДокумента | Сумма документа | Число 15.2 | Нет |
| Комментарий | Комментарий | Строка неограниченная | Нет |
| Ответственный | Ответственный | СправочникСсылка.Пользователи | Нет |

Тип основания: слева `ДокументСсылка`, справа `ЗаписьКлиента`.

У `Клиент` снова **Параметры выбора:** `Отбор.ТипКонтрагента` = `Клиент`.

### Шаг C. Две табличные части

Сначала ТЧ **Услуги** (как в записи, но колонка суммы называется `Сумма`):

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| Услуга | Услуга | СправочникСсылка.Номенклатура | Да |
| Сумма | Сумма | Число 15.2 | Да |

Параметры выбора Услуга: `Отбор.ТипНоменклатуры` = `Услуга`.

Затем ещё одна ТЧ **Товары**:

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| Товар | Товар | СправочникСсылка.Номенклатура | Да |
| Количество | Количество | Число, длина 15, точность 3 | Да |
| Цена | Цена | Число 15.2 | Да |
| Сумма | Сумма | Число 15.2 | Да |

Отбор «товар или материал» одним параметром выбора не ставится. На спринте 1 можно не ограничивать. На спринте 2 отбор сделаете в форме. Не создавайте второе перечисление.

### Шаг D. Ввод на основании записи клиента

1. В окне документа откройте вкладку **Ввод на основании** (иногда называется рядом с «Данные»).
2. В списке **Вводится на основании** поставьте галочку на `ЗаписьКлиента`.
3. Откройте **модуль объекта** документа: в окне документа кнопка **Модуль объекта** (или ПКМ по документу в дереве → **Открыть модуль объекта**).
4. Вставьте процедуру **целиком**, сохраните модуль:

```
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)

	Если ТипЗнч(ДанныеЗаполнения) = Тип("ДокументСсылка.ЗаписьКлиента") Тогда

		Основание = ДанныеЗаполнения;
		Клиент = ДанныеЗаполнения.Клиент;
		Сотрудник = ДанныеЗаполнения.Сотрудник;
		Комментарий = ДанныеЗаполнения.Комментарий;

		Для Каждого СтрокаИсточника Из ДанныеЗаполнения.Услуги Цикл
			СтрокаУслуги = Услуги.Добавить();
			СтрокаУслуги.Услуга = СтрокаИсточника.Услуга;
			СтрокаУслуги.Сумма = СтрокаИсточника.Стоимость;
		КонецЦикла;

	КонецЕсли;

КонецПроцедуры
```

Это методика урока «ввод на основании»: реализация подтягивает клиента, сотрудника, комментарий и строки услуг. Товары с записи не копируются — их в ТЗ нет.

`Ctrl+S`, `F7`. Движения конструктором — после регистров, в п. 1.17.

---

## 1.14. Документ ПоступлениеТоваров

1. ПКМ **Документы** → **Добавить**.
2. **Имя:** `ПоступлениеТоваров`. **Синоним:** `Поступление товаров`.
3. **Нумерация:** строка, 9, авто. Дата обязательна.
4. Реквизиты шапки (вкладка **Данные**, **+**):

| Имя | Синоним | Тип | Обязательный |
| --- | --- | --- | --- |
| ВходящийНомер | Номер входящего документа | Строка, 20 | Нет |
| ВходящаяДата | Дата входящего документа | Дата, состав «Дата» | Нет |
| Поставщик | Поставщик | СправочникСсылка.Контрагенты | Да |
| Договор | Договор | СправочникСсылка.ДоговорыКонтрагентов | Да |
| Склад | Склад | СправочникСсылка.Склады | Да |
| Комментарий | Комментарий | Строка неограниченная | Нет |
| Ответственный | Ответственный | СправочникСсылка.Пользователи | Нет |
| СуммаДокумента | Сумма документа | Число 15.2 | Да |

5. Реквизит `Поставщик` → **Параметры выбора** → `Отбор.ТипКонтрагента` = `Поставщик`.
6. Реквизит `Договор` документа: выделите его → **Связи параметров выбора** → **Открыть** → Добавить. Имя: `Отбор.Владелец`, реквизит: `Поставщик` (поле шапки этого же документа). Тогда в списке договоров только договоры выбранного поставщика. Подробные клики — в блоке «Как нажать Связи параметров выбора» выше.
7. Табличная часть **Товары** — те же колонки, что у реализации: Товар, Количество 15.3, Цена 15.2, Сумма 15.2, все обязательные.

`Ctrl+S`, `F7`.

---

## 1.15. Регистр накопления ТоварыНаСкладах

Регистр — это не список, который заполняют руками. В него пишут **документы** при проведении.

1. ПКМ по ветке **Регистры накопления** → **Добавить**.
2. **Имя:** `ТоварыНаСкладах`. **Синоним:** `Товары на складах`.
3. В палитре свойств регистра **Вид регистра:** `Остатки` (не Обороты).
4. Вкладка **Данные**.
5. Блок **Измерения** (не ресурсы) → **+** два раза:

| Имя | Синоним | Тип | Запрет незаполненных |
| --- | --- | --- | --- |
| Номенклатура | Номенклатура | СправочникСсылка.Номенклатура | Да (флажок в свойствах измерения) |
| Склад | Склад | СправочникСсылка.Склады | Да |

«Запрет незаполненных значений» — отдельный флажок в палитре измерения.

6. Блок **Ресурсы** → **+**:

| Имя | Синоним | Тип |
| --- | --- | --- |
| Количество | Количество | Число 15.3 |

7. Вкладка **Регистраторы**. Поставьте галочки только на:
   - `ПоступлениеТоваров`
   - `РеализацияТоваровИУслуг`

Если вкладки «Регистраторы» нет: свойство регистра **Регистраторы** → лупа → отметьте документы.

`Ctrl+S`, `F7`.

---

## 1.16. Регистр накопления Продажи

1. ПКМ **Регистры накопления** → **Добавить**.
2. **Имя:** `Продажи`. **Синоним:** `Продажи`.
3. **Вид регистра:** `Обороты`.
4. Измерения (все с запретом незаполненных):

| Имя | Тип |
| --- | --- |
| Номенклатура | СправочникСсылка.Номенклатура |
| Сотрудник | СправочникСсылка.Сотрудники |
| Клиент | СправочникСсылка.Контрагенты |

5. Ресурс: **Имя** `Сумма`, тип Число 15.2.
6. Регистратор: только `РеализацияТоваровИУслуг`.

`Ctrl+S`, `F7`.

---

## 1.17. Движения конструктором (обязательный приём спринта 1)

Не пишите проведение руками. Ревьюер смотрит, что код сделал **конструктор**.

### Поступление → приход на склад

1. Дважды откройте документ `ПоступлениеТоваров`.
2. Вкладка **Движения**.
3. В списке регистров поставьте галочку **ТоварыНаСкладах**.
4. Нажмите кнопку **Конструктор движений** (на этой же вкладке).
5. В конструкторе:
   - регистр: `ТоварыНаСкладах`
   - вид движения: **Приход**
   - табличная часть: **Товары**
6. В таблице соответствия выберите:
   - Номенклатура ← `Товар`
   - Склад ← `Склад` (реквизит шапки)
   - Количество ← `Количество`
7. Нажмите **Выполнить**. Согласитесь заменить / создать процедуру `ОбработкаПроведения`.
8. Откройте модуль объекта поступления и **посмотрите**: должен появиться цикл по ТЧ Товары и `ВидДвиженияНакопления.Приход`. Не переписывайте.

### Реализация → расход склада и обороты продаж

1. Откройте документ `РеализацияТоваровИУслуг` → вкладка **Движения**.
2. Галочки: **ТоварыНаСкладах** и **Продажи**.
3. **Конструктор движений**, первый заход:
   - регистр `ТоварыНаСкладах`, вид **Расход**, ТЧ **Товары**
   - Номенклатура ← Товар, Склад ← Склад, Количество ← Количество
   - **Выполнить**
4. Снова **Конструктор движений**, второй заход (не стирайте руками первый цикл, конструктор допишет):
   - регистр `Продажи`, ТЧ **Товары**
   - Номенклатура ← Товар, Сотрудник ← Сотрудник, Клиент ← Клиент, Сумма ← Сумма
   - **Выполнить**
5. Третий заход:
   - регистр `Продажи`, ТЧ **Услуги**
   - Номенклатура ← Услуга, Сотрудник, Клиент, Сумма ← Сумма
   - **Выполнить**

В модуле реализации на спринте 1 нормально иметь **два** цикла по ТЧ Товары. На спринте 2 вы сольёте их в один. Сейчас не оптимизируйте.

`Ctrl+S`, `F7`.

---

## 1.18. Печатная форма реализации

Учебный курс обычно печатает **командой формы**, без БСП. Делайте как на уроке печати. Ниже — тот же приём по шагам.

### Шаг A. Макет табличного документа

1. В дереве раскройте документ `РеализацияТоваровИУслуг` → ПКМ по **Макеты** → **Добавить**.
2. Тип макета: **Табличный документ**.
3. **Имя:** `ПФ_MXL_РеализацияТоваровИУслуг`.
4. **Синоним:** `Реализация товаров и услуг`.
5. Откроется сетка как в Excel.

Именованные области (обязательно, иначе код не найдёт куски):

1. Выделите несколько первых строк (например 1–8) мышью за номера строк слева.
2. ПКМ по выделенным номерам строк → **Свойства**.
3. **Имя:** `Заголовок`. Закройте свойства.
4. В ячейках этой области напишите заголовок `Реализация товаров и услуг` и подписи Номер, Дата, Клиент, Сотрудник, Основание.
5. В ячейках справа от подписей в свойствах ячейки (двойной щелчок или ПКМ → Свойства) поле **Заполнение** = **Параметр**, имена параметров: `Номер`, `Дата`, `Клиент`, `Сотрудник`, `Основание`.

Дальше так же именуйте области:

| Имя области | Что внутри |
| --- | --- |
| ШапкаУслуг | шапка таблицы: №, Услуга, Сумма |
| СтрокаУслуг | параметры `НомерСтроки`, `Услуга`, `Сумма` |
| ШапкаТоваров | №, Товар, Количество, Цена, Сумма |
| СтрокаТоваров | параметры `НомерСтроки`, `Товар`, `Количество`, `Цена`, `Сумма` |
| Подвал | подписи Сумма документа, Ответственный и параметры `СуммаДокумента`, `Ответственный` |

Параметр ячейки: свойства ячейки → Заполнение = Параметр → имя как в таблице.

### Шаг B. Команда Печать на форме документа

Пока основной формы нет, платформа откроет автоформу. Команду всё равно повесьте на документ:

1. ПКМ по документу в дереве → ветка **Команды** → **Добавить**.
2. **Имя:** `Печать`. **Синоним:** `Печать`.
3. **Группа:** командная панель формы.
4. **Тип параметра команды:** можно оставить ссылку на документ.
5. Откройте **модуль команды** (кнопка в окне команды).

Если на уроке команду ставили **на форму**, сделайте так после создания формы на спринте 2. На спринте 1 достаточно макета и вызова из модуля менеджера / команды.

Простой вариант «как в уроке» — общий модуль не обязателен. В **модуле менеджера** документа (ПКМ по документу → **Открыть модуль менеджера**) вставьте функцию и оставьте вызов из команды:

```
Функция СформироватьПечатнуюФорму(Документ) Экспорт

	ТабДок = Новый ТабличныйДокумент;
	Макет = Документы.РеализацияТоваровИУслуг.ПолучитьМакет("ПФ_MXL_РеализацияТоваровИУслуг");

	ОбластьЗаголовок = Макет.ПолучитьОбласть("Заголовок");
	ОбластьЗаголовок.Параметры.Заполнить(Документ);
	ТабДок.Вывести(ОбластьЗаголовок);

	Если Документ.Услуги.Количество() > 0 Тогда
		ТабДок.Вывести(Макет.ПолучитьОбласть("ШапкаУслуг"));
		ОбластьСтрока = Макет.ПолучитьОбласть("СтрокаУслуг");
		НомерСтроки = 0;
		Для Каждого СтрокаТЧ Из Документ.Услуги Цикл
			НомерСтроки = НомерСтроки + 1;
			ОбластьСтрока.Параметры.Заполнить(СтрокаТЧ);
			ОбластьСтрока.Параметры.НомерСтроки = НомерСтроки;
			ТабДок.Вывести(ОбластьСтрока);
		КонецЦикла;
	КонецЕсли;

	Если Документ.Товары.Количество() > 0 Тогда
		ТабДок.Вывести(Макет.ПолучитьОбласть("ШапкаТоваров"));
		ОбластьСтрока = Макет.ПолучитьОбласть("СтрокаТоваров");
		НомерСтроки = 0;
		Для Каждого СтрокаТЧ Из Документ.Товары Цикл
			НомерСтроки = НомерСтроки + 1;
			ОбластьСтрока.Параметры.Заполнить(СтрокаТЧ);
			ОбластьСтрока.Параметры.НомерСтроки = НомерСтроки;
			ТабДок.Вывести(ОбластьСтрока);
		КонецЦикла;
	КонецЕсли;

	ОбластьПодвал = Макет.ПолучитьОбласть("Подвал");
	ОбластьПодвал.Параметры.СуммаДокумента = Документ.СуммаДокумента;
	ОбластьПодвал.Параметры.Ответственный = Документ.Ответственный;
	ТабДок.Вывести(ОбластьПодвал);

	Возврат ТабДок;

КонецФункции
```

Модуль команды (клиент):

```
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	ТабДок = ПечатьНаСервере(ПараметрКоманды);
	ТабДок.Показать("Реализация товаров и услуг");
КонецПроцедуры

&НаСервере
Функция ПечатьНаСервере(Документ)
	Возврат Документы.РеализацияТоваровИУслуг.СформироватьПечатнуюФорму(Документ);
КонецФункции
```

Если платформа ругается, что в модуле команды нельзя `&НаСервере`, вынесите серверный вызов в общий модуль с флажком «Вызов сервера», как показывали на уроке. **Не** копируйте `УправлениеПечатью` из БСП — в учебной базе этой подсистемы обычно нет.

Пропуск пустых ТЧ (`Если Количество() > 0`) сразу заложите: на спринте 2 это потребуют явно.

`Ctrl+S`, `F7`.

---

## 1.19. Функциональная опция складов

Два объекта: константа (флажок в базе) и функциональная опция (прячет связанные объекты, когда флажок снят).

### Константа

1. ПКМ **Константы** → **Добавить**.
2. **Имя:** `ИспользоватьСклады`. **Синоним:** `Использовать склады`.
3. **Тип:** Булево.

### Опция

1. ПКМ **Функциональные опции** → **Добавить**.
2. **Имя:** `ИспользоватьСклады`. **Синоним:** `Использовать склады`.
3. Свойство **Хранение:** выберите константу `ИспользоватьСклады` (лупа).
4. Вкладка **Состав** (или свойство Состав → лупа). Отметьте связанные объекты:
   - справочник `Склады`
   - реквизит `Склад` документа `РеализацияТоваровИУслуг`
   - реквизит `Склад` документа `ПоступлениеТоваров`
   - регистр `ТоварыНаСкладах`
   - измерение `Склад` этого регистра

`Ctrl+S`, `F7`.

В 1С:Предприятие пока включить флажок: главное меню (кружок) → **Все функции** (если пункта нет: Сервис → Параметры → «Отображать команду Все функции») → **Константы** → `Использовать склады` = Да. На спринте 2 вынесете это в подсистему Настройки.

---

## 1.20. Что проверить в режиме 1С:Предприятие

Данные для полей — в [03-dannye.md](03-dannye.md). Как открыть объекты без подсистем спринта 2: **Все функции** → **Справочники** / **Документы**.

1. Породы: **Ещё** → Режим просмотра → **Дерево**, папки Кошки и Собаки на месте, внутри хотя бы по три породы.
2. Контрагенты: дерево, папки Клиенты и Поставщики, внутри клиенты и поставщики.
3. Договор с владельцем ООО «Бонифаций». В поступлении список договоров не показывает чужие.
4. Запись клиента проведена (кнопка **Провести**; на спринте 1 движений заказов ещё нет — это нормально).
5. Из записи: **Создать на основании** → Реализация: клиент, сотрудник, услуги, комментарий подставились.
6. Поступление проведено. **Все функции** → Регистр накопления **Товары на складах** → есть приход.
7. Реализация проведена: по складу расход, в регистре **Продажи** строки услуг и товаров.
8. Кнопка **Печать** открывает табличный документ.
9. Константа складов: снимите флажок, перезайдите — склады пропадают из интерфейса. Верните флажок.

Код `ОбработкаПроведения` оставьте конструкторским, на русском. Не «улучшайте» его в спринте 1.
